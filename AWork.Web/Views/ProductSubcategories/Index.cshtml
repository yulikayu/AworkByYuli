@*@model IEnumerable<AWork.Contracts.Dto.Production.ProductSubCategoryDto>

@{
    ViewData["Title"] = "Index";
}


 <div class="row">
     <h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>

<div class="card">
                <h5 class="card-header">Responsive Table</h5>
                <div class="table-responsive text-nowrap">
                  <table class="table">
                    <thead>
                      <tr class="text-nowrap">
                        <th>No</th>
                        <th>@Html.DisplayNameFor(model => model.Name)</th>
                        <th>@Html.DisplayNameFor(model => model.Rowguid)</th>
                        <th> @Html.DisplayNameFor(model => model.ModifiedDate)</th>
                        <th>@Html.DisplayNameFor(model => model.ProductCategoryId)</th>
                        <th>@Html.DisplayNameFor(model => model.ProductSubcategoryId)</th>
                         <th>Action</th>
                      </tr>
                    </thead>
                    <tbody class="table-border-bottom-0">
                      @foreach (var item in Model) {
                        <tr>
                            <td>
                                @Html.DisplayFor(modelItem => item.Name)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.Rowguid)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.ModifiedDate)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.Name)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.ProductCategoryId)
                            </td>
                             <td>
                                @Html.DisplayFor(modelItem => item.ProductSubcategoryId)
                            </td>
                            <td>
                                <a  asp-action="Edit" asp-route-id="@item.ProductSubcategoryId">Edit</a> |
                                <a  asp-action="Details" asp-route-id="@item.ProductSubcategoryId">Details</a> |
                                <a  asp-action="Delete" asp-route-id="@item.ProductSubcategoryId">Delete</a>
                            </td>
                        </tr>
                }
                    </tbody>
                  </table>
                </div>
              </div>
</div>

*@

@model IEnumerable<AWork.Contracts.Dto.Production.ProductCategoryDto>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Rowguid)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ModifiedDate)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Rowguid)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ModifiedDate)
            </td>
            <td>
                <a asp-controller="ProductSubcategories" asp-action="Select" asp-route-id="@item.ProductCategoryId">Select</a>|
                <a asp-action="Edit" asp-route-id="@item.ProductCategoryId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ProductCategoryId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ProductCategoryId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
